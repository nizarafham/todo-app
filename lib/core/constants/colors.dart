import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF2D336B);
  static const Color primaryVariant = Color(0xFF3700B3);

  // Secondary Colors
  static const Color secondary = Color(0xFF7886C7);
  static const Color secondaryVariant = Color(0xFF018786);

  // Background Colors
  static const Color background = Color(0xFFFFF2F2);
  static const Color surface = Color(0xFFA9B5DF);

  // Error Colors
  static const Color error = Color.fromARGB(255, 176, 0, 0);

  // Text Colors
  static const Color onPrimary = Color(0xFFFFFFFF);
  static const Color onSecondary = Color.fromARGB(255, 245, 243, 243);
  static const Color onBackground = Color(0xFF000000);
  static const Color onSurface = Color(0xFF000000);
  static const Color onError = Color(0xFFFFFFFF);

  // Additional Colors
  static const Color accent = Color.fromARGB(255, 189, 22, 22);
  static const Color disabled = Color(0xFF9E9E9E);
}